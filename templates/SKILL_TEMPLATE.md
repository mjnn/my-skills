---
name: {skill-name}
description: 当用户{具体触发场景}时触发。涵盖{核心功能}。不适用于：{排除场景1}、{排除场景2}、{排除场景3}。
---

# {技能标题}

{一句话定位}

<HARD-GATE>
1. 在用户审核 Spec 并调用 ExitSpecMode 确认前，禁止执行任何写操作。
2. 涉及向用户收集信息时，必须使用 AskUserQuestion 工具提供结构化选项，禁止纯文本提问。
3. 涉及执行动作时，必须走 Spec 模式（EnterSpecMode → ExitSpecMode）。
</HARD-GATE>

## Gotchas

### 1. {具体错误}

{描述 Agent 具体会犯什么错误}

**纠正**：{Agent 应该怎么做}

### 2. {具体错误}

{描述 Agent 具体会犯什么错误}

**纠正**：{Agent 应该怎么做}

### 3. {具体错误}

{描述 Agent 具体会犯什么错误}

**纠正**：{Agent 应该怎么做}

## 流程

### 阶段一：前置检查

1. {检查项 1}
2. {检查项 2}
3. {检查项 3}

### 阶段二：信息收集

使用 AskUserQuestion 工具收集以下信息：

| 问题 | header | 选项 |
|------|--------|------|
| {问题1} | {header1} | {选项A} / {选项B} / {选项C} |
| {问题2} | {header2} | {选项A} / {选项B} / {选项C} |

### 阶段三：Spec 模式计划审核

信息收集完成后，必须调用 EnterSpecMode 进入 spec 模式。

Spec 内容必须包含：
1. {计划中必须包含的内容项}
2. {关键决策点}
3. {预期输出格式}

用户审核后调用 ExitSpecMode 确认，才能进入下一阶段。

### 阶段四：执行

**步骤 1：{步骤名称}**

```bash
{命令}
```

**步骤 2：{步骤名称}**

```bash
{命令}
```

### 阶段五：异常处理

**{异常场景 1}**：
- {处理方式}

**{异常场景 2}**：
- {处理方式}

## references

- `references/{file}.md`：{用途说明}

## 执行后复盘（自迭代钩子）

每次完成本 skill 的全部步骤后，Agent 必须自动执行以下动作，不询问用户：

1. **反思**：本轮执行中，是否遇到了 skill 正文和 Gotchas 都没覆盖的坑？

2. **记录**：有则追加到 `evals/PITFALLS_LOG.md`，无则跳过。

| 日期 | 场景摘要 | 犯了什么错误 | 如果早知道__就不会犯 | 临时补救方式 |
|------|---------|-------------|-------------------|------------|
| YYYY-MM-DD | 一句话 | Agent 具体做了什么 | 缺失的知识/规则 | 怎么绕过去的 |

3. **不提交**：`PITFALLS_LOG.md` 是个人本地文件，不提交到 skill registry。
